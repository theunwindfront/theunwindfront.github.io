name = "aushadhi-api"
main = "worker.js"
compatibility_date = "2026-01-01"

# Optional per-IP rate limiting. Create the namespace, then paste its id here:
#   npx wrangler kv namespace create RATE
# [[kv_namespaces]]
# binding = "RATE"
# id = "<paste-id-here>"
